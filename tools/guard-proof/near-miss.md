# Near-miss for the Trojan Source guard

This file exists for one commit. The line below carries U+202E, a
right-to-left override, which is the character `unicode-guard` is written
against: it reorders what a reader sees without changing what a compiler
reads.

    the bytes after this arrow are overridden: ‮gnitset si sihT

The commit that follows removes this directory.
